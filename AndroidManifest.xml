<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.wswld.dtype.writer"
    android:versionCode="61"
    android:versionName="@string/version" >

    <uses-sdk android:minSdkVersion="7" android:maxSdkVersion="17" android:targetSdkVersion="10"/>

    <application
        android:icon="@drawable/dtype_icon"
        android:label="@string/app_name" android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">
        <activity android:name=".DTypeActivity" android:label="@string/app_name" android:theme="@android:style/Theme.Light.NoTitleBar.Fullscreen" android:icon="@drawable/dtype_icon">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity><activity android:name=".IntroSplashActivity" android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen" android:launchMode="standard"></activity>
        <activity android:theme="@android:style/Theme.Light.NoTitleBar.Fullscreen" android:name="OptionsActivity"></activity>
        
    </application>

</manifest>